<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.ShoppingCartMapper">
    <insert id="insertBatch">
        insert into shopping_cart (name, user_id, dish_id, setmeal_id, dish_flavor, number, amount, image, create_time)
        values
        <foreach collection="list" item="item" separator=",">
            (#{item.name},#{item.userId},#{item.dishId},#{item.setmealId},#{item.dishFlavor},#{item.number},#{item.amount},#{item.image},#{item.createTime})
        </foreach>
    </insert>
    <delete id="deleteByUserId">
        delete from shopping_cart where user_id = #{userId}
    </delete>
    <!--注意：动态sql里面，上面这个判断条件写的是实体类中的属性名，
            下面的sql写的是表中的字段名。-->
    <select id="list" parameterType="com.sky.entity.ShoppingCart" resultType="com.sky.entity.ShoppingCart">
        select id,name,user_id,dish_id,setmeal_id,dish_flavor,number,amount,image,create_time from shopping_cart
        <where>
            <if test="userId != null">
                and user_id = #{userId}
            </if>
            <if test="dishId != null">
                and dish_id = #{dishId}
            </if>
            <if test="setmealId != null">
                and setmeal_id = #{setmealId}
            </if>
            <if test="dishFlavor != null">
                and dish_flavor = #{dishFlavor}
            </if>
        </where>
        order by create_time desc
    </select>
</mapper>
